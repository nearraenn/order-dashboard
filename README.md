# Order Dashboard

Responsive React + TypeScript implementation of the ATA-IT frontend assignment.

## Run locally

```bash
npm install
npm run dev
```

## Production check

```bash
npm run lint
npm run build
```

The order data is mocked in `src/data/orders.ts`. The search form filters by the selected inclusive date range. Period and status are intentionally fixed to the MVP values from the assignment.
